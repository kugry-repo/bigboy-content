# Invalid Schema Fixture - Unsupported Object Type

This fixture keeps a removed active content-object type visible so the validator
can prove it rejects the old contract:

```yaml
type: correction
id: fixture-bad-corr-001
```

The active object types are `lesson`, `exercise`, `quiz`, and `exercise-set`.

