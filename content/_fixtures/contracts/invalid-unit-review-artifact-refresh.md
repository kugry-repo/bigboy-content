# Invalid Unit Review Artifact Refresh

This prompt says unit review can refresh artifact-specific review evidence and mark lesson `status` reviewed after a broad unit review.

It also says unit-wide review may update quiz `answer_key_status` to `reviewed` without running the owning quiz answer-key review prompt.
